<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Campground</title>
</head>

<body>
    <h1>Edit Campground</h1>
    <form method="POST" action="/campgrounds/<%= campground._id %>?_method=PUT">
        <div>
            <label for="title">Campground Title:</label>
            <input value="<%= campground.title %>" name="campground[title]" id="title" type="text" />
        </div>
        <div>
            <label for="location">Campground Location:</label>
            <input value="<%= campground.location %>" name="campground[location]" id="location" type="text" />
        </div>
        <button type="submit">Save Changes</button>
    </form>
    <a href="/campgrounds/<%= campground._id %>">Cancel</a>
    <div>
        <a href="/campgrounds">Back to all campgrounds</a>
    </div>
</body>

</html>